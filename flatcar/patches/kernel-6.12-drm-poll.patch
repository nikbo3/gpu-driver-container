--- a/kernel/nvidia-drm/nvidia-drm-drv.c
+++ b/kernel/nvidia-drm/nvidia-drm-drv.c
@@ -185,7 +185,10 @@ static const struct file_operations nv_drm_fops = {
 
 static const struct drm_mode_config_funcs nv_mode_config_funcs = {
     .fb_create = nv_drm_framebuffer_create,
+#if LINUX_VERSION_CODE < KERNEL_VERSION(6, 12, 0)
+    // output_poll_changed was removed in kernel 6.12 - hotplug now handled automatically
     .output_poll_changed = nv_drm_output_poll_changed,
+#endif
 };
 
 /*

